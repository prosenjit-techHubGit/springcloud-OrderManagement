server:
  port: 8888
  
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/prosenjit-techHubGit/app-config-repo.git
          username: prosenjit-techHubGit
          password: Appian6$
          skip-ssl-validation: true