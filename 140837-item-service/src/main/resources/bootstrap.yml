spring:
  application:
    name: 140837-item-service
  profiles:
    active:
    - dev
  cloud:
    config:
      uri:
      - http://localhost:8888
server:
  servlet:
    context-path: /service          